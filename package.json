{
  "name": "jazz-spotify-tools",
  "version": "1.0.0",
  "description": "Tools for verifying and managing Spotify links in the Jazz book",
  "private": true,
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "validate-spotify": "ts-node tools/validate-spotify-links.ts",
    "verify-spotify": "ts-node tools/verify-spotify-links.ts",
    "check": "npm run build && npm test"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.19",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "axios": "^1.6.7"
  }
}
